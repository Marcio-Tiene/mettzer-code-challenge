export interface IFormData {
  authors: string;
  title: string;
}

export interface IData {
  id: string;
  authors: string[];
  type: string;
  description: string;
  title: string;
  urls: string[];
}
